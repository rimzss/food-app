import AfterForgetPage from "@/components/pages/Forget/afterCode";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return <AfterForgetPage />;
};

export default page;
